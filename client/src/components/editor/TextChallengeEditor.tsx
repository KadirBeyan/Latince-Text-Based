import type { TextChallenge } from "../../types/gameTypes";
export function TextChallengeEditor({ value, onChange }: { value?: TextChallenge | null; onChange: (value: TextChallenge | null) => void }) { if (!value) return <button onClick={() => onChange({id:`challenge-${Date.now()}`,prompt:"",expectedAnswers:[]})}>Text challenge ekle</button>; return <section><h4>Text Challenge</h4><textarea value={value.prompt} onChange={e => onChange({...value,prompt:e.target.value})}/><input placeholder="Cevaplar; ile" value={(value.expectedAnswers ?? []).join("; ")} onChange={e => onChange({...value,expectedAnswers:e.target.value.split(";").map(x=>x.trim()).filter(Boolean)})}/><button onClick={() => onChange(null)}>Kaldır</button></section>; }
