import { useState } from "react";
import type { AssessmentQuestion } from "../../types/assessmentTypes";
export function AssessmentQuestionCard({ question, disabled, onSubmit }: { question: AssessmentQuestion; disabled?: boolean; onSubmit: (answer: { answerText?: string; selectedChoice?: string }) => void }) { const [value, setValue] = useState(""); const isChoice = question.type === "multiple-choice"; return <div className="panel-card assessment-card"><div className="panel-kicker">{question.level} · {question.difficulty}</div><h3>{question.promptTr}</h3>{question.latinText ? <p className="latin-quote">{question.latinText}</p> : null}{isChoice ? <div className="choice-list">{(question.choices ?? []).map((choice) => <button key={choice} type="button" className={value === choice ? "active" : ""} onClick={() => setValue(choice)} disabled={disabled}>{choice}</button>)}</div> : <input className="latin-input-field" value={value} onChange={(event) => setValue(event.target.value)} disabled={disabled} placeholder="Cevabını yaz" />}<button type="button" className="primary-action" disabled={disabled || !value.trim()} onClick={() => onSubmit(isChoice ? { selectedChoice: value } : { answerText: value })}>Cevapla</button></div>; }

