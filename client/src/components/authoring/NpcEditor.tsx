import { VpCard, VpSectionHeader, VpTextarea } from "../ui";
import { Field, JsonBlock, split } from "./SceneEditor";

export function NpcEditor({ data, onChange }: { data: any; onChange: (patch: any) => void }) {
  return <div className="authoring-editor-stack"><VpCard variant="compact"><VpSectionHeader eyebrow="NPC" title={data?.name ?? "Persona"} /><div className="authoring-form-grid"><Field label="id" value={data?.id} onChange={(id) => onChange({ id })} /><Field label="name" value={data?.name} onChange={(name) => onChange({ name })} /><Field label="latinName" value={data?.latinName} onChange={(latinName) => onChange({ latinName })} /><Field label="role" value={data?.role} onChange={(role) => onChange({ role })} /><Field label="defaultLocationId" value={data?.defaultLocationId} onChange={(defaultLocationId) => onChange({ defaultLocationId })} /><Field label="teachingStyle" value={data?.teachingStyle} onChange={(teachingStyle) => onChange({ teachingStyle })} /></div><label className="authoring-wide"><span>descriptionTr</span><VpTextarea value={data?.descriptionTr ?? data?.description ?? ""} onChange={(event) => onChange({ description: event.target.value, descriptionTr: event.target.value })} /></label><div className="authoring-form-grid"><Field label="preferredGrammarIds" value={(data?.preferredGrammarIds ?? []).join(", ")} onChange={(value) => onChange({ preferredGrammarIds: split(value) })} /><Field label="preferredVocabularyIds" value={(data?.preferredVocabularyIds ?? []).join(", ")} onChange={(value) => onChange({ preferredVocabularyIds: split(value) })} /></div></VpCard><JsonBlock data={data} onChange={onChange} /></div>;
}
