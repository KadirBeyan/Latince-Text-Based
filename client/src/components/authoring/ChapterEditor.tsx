import { VpCard, VpSectionHeader } from "../ui";
import { Field, JsonBlock, split } from "./SceneEditor";
import { CinematicPreview } from "./CinematicPreview";

export function ChapterEditor({ data, onChange }: { data: any; onChange: (patch: any) => void }) {
  return <div className="authoring-editor-stack"><VpCard variant="compact"><VpSectionHeader eyebrow="Chapter" title={data?.titleTr ?? data?.title ?? "Chapter"} /><div className="authoring-form-grid"><Field label="id" value={data?.id} onChange={(id) => onChange({ id })} /><Field label="titleTr" value={data?.titleTr ?? data?.title} onChange={(title) => onChange({ title, titleTr: title })} /><Field label="titleLatin" value={data?.titleLatin} onChange={(titleLatin) => onChange({ titleLatin })} /><Field label="order" value={data?.order ?? ""} onChange={(order) => onChange({ order: Number(order) || order })} /><Field label="startSceneId" value={data?.startSceneId} onChange={(startSceneId) => onChange({ startSceneId })} /><Field label="startQuestId" value={data?.startQuestId} onChange={(startQuestId) => onChange({ startQuestId })} /></div></VpCard><CinematicPreview kind="chapter" data={data} /><VpCard variant="compact"><VpSectionHeader eyebrow="Coverage" title={`${data?.quests?.length ?? 0} quests`} /><div className="authoring-form-grid"><Field label="locationIds" value={(data?.locationIds ?? []).join(", ")} onChange={(value) => onChange({ locationIds: split(value) })} /><Field label="npcIds" value={(data?.npcIds ?? []).join(", ")} onChange={(value) => onChange({ npcIds: split(value) })} /><Field label="grammarFocus" value={(data?.grammarFocus ?? []).join(", ")} onChange={(value) => onChange({ grammarFocus: split(value) })} /><Field label="vocabularyFocus" value={(data?.vocabularyFocus ?? []).join(", ")} onChange={(value) => onChange({ vocabularyFocus: split(value) })} /></div></VpCard><JsonBlock data={data} onChange={onChange} /></div>;
}
