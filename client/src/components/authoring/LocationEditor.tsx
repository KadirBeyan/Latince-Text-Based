import { VpCard, VpSectionHeader, VpTextarea } from "../ui";
import { Field, JsonBlock, split } from "./SceneEditor";

export function LocationEditor({ data, onChange }: { data: any; onChange: (patch: any) => void }) {
  return <div className="authoring-editor-stack"><VpCard variant="compact"><VpSectionHeader eyebrow="Location" title={data?.titleTr ?? data?.title ?? "Locus"} /><div className="authoring-form-grid"><Field label="id" value={data?.id} onChange={(id) => onChange({ id })} /><Field label="title" value={data?.titleTr ?? data?.title} onChange={(title) => onChange({ title, titleTr: title })} /><Field label="titleLatin" value={data?.titleLatin} onChange={(titleLatin) => onChange({ titleLatin })} /><Field label="mood" value={data?.mood} onChange={(mood) => onChange({ mood })} /><Field label="connectedLocationIds" value={(data?.connectedLocationIds ?? []).join(", ")} onChange={(value) => onChange({ connectedLocationIds: split(value) })} /><Field label="defaultNpcIds" value={(data?.defaultNpcIds ?? []).join(", ")} onChange={(value) => onChange({ defaultNpcIds: split(value) })} /><Field label="grammarIds" value={(data?.grammarIds ?? []).join(", ")} onChange={(value) => onChange({ grammarIds: split(value) })} /><Field label="vocabularyIds" value={(data?.vocabularyIds ?? []).join(", ")} onChange={(value) => onChange({ vocabularyIds: split(value) })} /></div><label className="authoring-wide"><span>descriptionTr</span><VpTextarea value={data?.descriptionTr ?? data?.description ?? ""} onChange={(event) => onChange({ descriptionTr: event.target.value })} /></label></VpCard><JsonBlock data={data} onChange={onChange} /></div>;
}
